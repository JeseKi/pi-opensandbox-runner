# Optional remote MCP credentials. Use with:
#   ./scripts/up.sh alice --mcp-env-file .sandbox.env
# Only MCP_* variables are accepted; provider keys belong in .litellm.env.
# MCP_CONTEXT7_TOKEN=
# MCP_GITHUB_TOKEN=
# MCP_ALLOW_INSECURE_HTTP=1
